using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CreateCharactorManeger : MonoBehaviour
{

    public static bool SelectIdouFlag,StopFlag,TyuuiFlag,AnimationPlaying;

    // Start is called before the first frame update
    void Start()
    {
        //Time.timeScale = 0;

        StopFlag = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
